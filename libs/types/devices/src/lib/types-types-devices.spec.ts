import { typesTypesDevices } from './types-types-devices';

describe('typesTypesDevices', () => {
  it('should work', () => {
    expect(typesTypesDevices()).toEqual('types-types-devices');
  });
});
